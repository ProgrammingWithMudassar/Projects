import RecoverPassword from "@/views/Auth/RecoverPassword";

export const metadata = {
    title: 'Recover password | Konnect.io',
    description: 'Recover password | Konnect.io',
}

const page = () => {
    return <RecoverPassword />
}

export default page;